{
  "name": "minecraft-backend",
  "version": "1.0.0",
  "description": "backend to receive webhooks and launch command into pod",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "adrien.eppling@supinfo.com",
  "license": "ISC",
  "dependencies": {
    "child_process": "^1.0.2",
    "express": "^4.18.3"
  }
}
